#ifndef XmClientLibs
#define XmClientLibs $(XMLIB) $(XTOOLLIB) $(XLIB)
#endif

#include "../demos.cf"

EXTRA_INCLUDES = -I.

SRCS = colordemo.c bxutil-c.c 
OBJS = colordemo.o bxutil-c.o 
LOCAL_LIBRARIES = -lXm -lXt -lXp -lX11 -lXext

ComplexProgramTarget(colordemo)


